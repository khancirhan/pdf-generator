root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/server ./cmd/server"
  bin = "./tmp/server"
  delay = 1000
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "testdata", "assets"]
  exclude_regex = ["_test.go"]

[misc]
  clean_on_exit = true